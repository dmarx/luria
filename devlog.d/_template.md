<!-- One devlog fragment per contribution, named devlog.d/<branch-slug>.md —
     the same convention as changelog.d/. Write the entry exactly as it should
     read in the collected log; collection appends the body verbatim, in the
     order the fragments were committed.

     The devlog is narrative, not a changelog: what problem was solved, what the
     fix was, and what was found along the way — root-cause archaeology, failed
     approaches, verification details, the traps the next person would otherwise
     rediscover. **Record the wrong theories and why they were wrong.** They are
     the expensive part and the part that never appears in a commit message.

     Entries are bold-lead paragraphs:

     **Short title of the work (#issue).** What was reported, what it actually
     turned out to be, how it was verified, and what surprised you.

     No fragment is required — the devlog covers significant work, not every
     contribution. Delete this file's content and write yours, or don't add a
     fragment at all. -->
