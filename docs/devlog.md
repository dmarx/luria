# Development log

The narrative that doesn't fit a changelog entry: root-cause archaeology, failed
approaches, verification details, and the traps the next person would otherwise
rediscover. Assembled from `devlog.d/` fragments — never hand-edited.

<!-- luria-insert-here -->
