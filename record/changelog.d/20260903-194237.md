### Changed

- Generated views are committed on the default branch only. A pull request
  regenerates them in the working tree, lints the regenerated tree in the
  same job, and commits nothing — so branches never conflict on the
  decision index or the devlog book. The generate action takes
  `commit: "false"` for that shape; the scaffold's workflow uses it.
