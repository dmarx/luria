<!-- No user-facing changes: this contribution is a Proposed decision and
     nothing else. It changes no behaviour, adds no command and alters no
     output — the question it asks is whether to build what it describes, and
     the merge verdict is the answer (ADR-052). A fragment that collected to
     an entry here would announce a feature that does not exist. -->
