### Added

- **This record now shows a map of itself.** `[luria.site] graph` points at
  `docs/graphs/record-map.json` — every decision and principle cited five or
  more times, drawn in strata-g from the record itself, each node linking to
  its own page. [`docs/graphs/README.md`](../../docs/graphs/README.md) has the
  recipe for redrawing it.
