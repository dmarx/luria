---
status: Proposed
formerly:
- ADR-tmpttexr
title: A scheme-level finding needs a scheme-level acknowledgement
version: 1
tags:
- record
- mechanism
date: '2026-08-16'
summary: >-
  `inert-status` was the only judgment call in luria a project could not
  answer. Every other has an `inactive-ok:`-style directive carrying a reason,
  at the site that raises it; this one is about a scheme, so it has no site,
  and a project whose uniformity was deliberate had no move that would ever
  quiet it. `uniform_ok` on the scheme is that acknowledgement: the row moves
  from finding to note and keeps its reason. Rejected: silencing it by
  declaring a one-status vocabulary, which asserts something false; and
  dropping the check, which is right about what it reports.
---

# ADR-059: A scheme-level finding needs a scheme-level acknowledgement

## Context

Every judgment call in this tool can be answered where it is raised. A retired
citation takes `inactive-ok:`, an unresolvable code takes `unresolved-ok:`, a
bare URL takes `url-ok:`, a broad title takes `broad-ok:`. The bargain is always
the same: the reason is mandatory, it lives at the site, and it renders where
the finding would have. A reader learns both that nobody is checking and why
nobody needs to.

`inert-status` ([#104](https://github.com/dmarx/luria/issues/104)) broke that bargain, and not by oversight — it is about a
*scheme*, and a scheme has no line to comment on. So a project whose uniformity
was deliberate had no move available. The check fired on every run, forever, and
the only two responses were to ignore it or to make it false.

**A guard nobody can answer is one people learn to skip**, which is the failure
mode the acknowledgement directives exist to prevent. It showed up downstream:
`mathematics-of-meaning` has run this warning on every lint invocation for
weeks, on two schemes, with no action available that would ever quiet it.

## Decision

A scheme may set `uniform_ok` in `luria.toml`, a string:

```toml
[luria.schemes.ADR]
dir = "record/decisions.d"
uniform_ok = "Twelve decisions, weeks old, none yet superseded — the expected state for a record this young. Revisit at the first supersession."
```

Set, the scheme drops out of `inert-status` and appears in a new
`acknowledged-uniformity` section, **which still reports the count and the
status** and appends the reason. The fact does not stop being true because
someone explained it: nothing in that scheme is being judged, and the citation
checks still cannot fire off it. It stops being a *finding*.

Three properties follow, and each is the point rather than a detail:

- **The reason is mandatory.** An empty string is no acknowledgement. Silence
  that carries no argument is indistinguishable from an oversight, which is
  exactly what this replaces.
- **The acknowledgement lapses on its own.** It suppresses only while the
  scheme is *actually* uniform. The first record filed at a second status and
  the row leaves both sections — no stale directive to clean up, because the
  condition, not the annotation, is what is tracked.
- **A project cannot promote it to a failure.** `acknowledged-uniformity` is
  absent from `FAILABLE`, so naming it in `fail_on` is caught by the existing
  unknown-class check rather than silently enforcing nothing.

## Alternatives rejected

**Declare a one-status vocabulary.** A `statuses.yaml` with a single entry
already suppresses the row, by design — "a project saying so on purpose". It is
the wrong instrument here. Saying that `ADR` has one status asserts that a
decision can never be superseded, which is false, and would hide the finding on
the day it became real. The suppression and the claim have to be separable.

**Scope the check off per scheme with a boolean.** `uniform_ok = true` would
work mechanically and drops the only thing worth keeping. Every other
acknowledgement in this tool carries prose, for the same reason: the annotation
is a claim about the world and has to be readable as one.

**Drop the check.** It is right about what it reports, and the downstream case
proves it: of the two schemes it fires on there, one is a young record and one
turns out to hold a concept that project's own claims say should be retired. A
check with a real hit rate does not need removing. It needs an answer.

**Raise `FLOOR`.** A count is a bad proxy for maturity, and tuning it trades one
arbitrary threshold for another while still leaving a project no way to speak.
